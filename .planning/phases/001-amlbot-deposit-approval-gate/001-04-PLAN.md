---
phase: 001-amlbot-deposit-approval-gate
plan: 04
type: execute
wave: 3
depends_on: ["001-01", "001-02", "001-03"]
files_modified:
  - "tests/conftest.py"
  - "tests/test_aml_end_to_end.py"
  - "tests/test_aml_contract_docs.py"
  - "docs/amlbot_deposit_gate.md"
  - "README.md"
  - "/Users/test/PycharmProjects/aml-shkeeper/tests/test_shkeeper_contract.py"
autonomous: true
requirements: ["SPEC-01", "SPEC-02", "SPEC-04", "SPEC-05", "SPEC-06", "SPEC-07", "SPEC-08", "SPEC-09", "SPEC-10", "SPEC-11", "SPEC-12", "SPEC-13", "SPEC-14", "SPEC-15"]
user_setup:
  - "Live AMLBot staging smoke test is manual and optional; automated tests must mock provider responses."
must_haves:
  truths:
    - "All SPEC requirements have automated unit or integration coverage where possible."
    - "D-09/D-13 cost thresholds are documented as defaults and configurable values, not hard compliance guarantees."
    - "D-14 KYT/address monitoring remains deferred until non-per-transaction pricing is confirmed."
    - "SPEC-15: docs state SHKeeper never credits grither-pay balances and manual review belongs in grither-pay."
  artifacts:
    - "tests/test_aml_end_to_end.py"
    - "tests/test_aml_contract_docs.py"
    - "docs/amlbot_deposit_gate.md"
    - "/Users/test/PycharmProjects/aml-shkeeper/tests/test_shkeeper_contract.py"
  key_links:
    - "SHKeeper contract tests use the same deposit_id/idempotency_key fields as aml-shkeeper sidecar tests."
    - "Docs describe trigger-transaction crediting, manual_review routing, and static-address PARTIAL invoice behavior."
---

<objective>
Add end-to-end verification and operator-facing documentation for the AMLBot deposit gate.

Purpose: This feature gates real money movement through callbacks; the plan must prove callback behavior, idempotency, crypto coverage, and grither-pay responsibilities are documented and testable.
Output: SHKeeper test fixtures, integration-style tests, sidecar contract tests, AML callback contract docs, and README link.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/001-amlbot-deposit-approval-gate/001-SPEC.md
@.planning/phases/001-amlbot-deposit-approval-gate/001-CONTEXT.md
@.planning/phases/001-amlbot-deposit-approval-gate/001-RESEARCH.md
@.planning/phases/001-amlbot-deposit-approval-gate/001-VALIDATION.md
@shkeeper/api_v1.py
@shkeeper/callback.py
@shkeeper/models.py
@README.md
</context>

<threat_model>
Trust boundaries:
- Test fixtures simulating coin sidecars, aml-shkeeper, and grither-pay callbacks.
- Documentation boundary where grither-pay developers implement credit/manual-review behavior from the callback contract.

STRIDE register:
| ID | Threat | Component | Mitigation |
|----|--------|-----------|------------|
| T-04-01 | Tests pass while callback contract is undocumented | docs/test link | Add doc tests that assert required contract strings are present. |
| T-04-02 | E2E flow double-credits on retry | callback retry tests | Assert stable idempotency_key and single callback_confirmed transition. |
| T-04-03 | Unsupported crypto silently credits in test gaps | coverage tests | Include enabled-crypto coverage and unsupported manual_review tests. |
| T-04-04 | Operators misunderstand pending vs available balance | docs | Document pending/manual_review/credit responsibilities clearly. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add SHKeeper test fixtures and end-to-end AML flow tests</name>
  <files>tests/conftest.py, tests/test_aml_end_to_end.py</files>
  <read_first>shkeeper/__init__.py, shkeeper/models.py, shkeeper/api_v1.py, shkeeper/callback.py, shkeeper/services/aml_processing.py, .planning/phases/001-amlbot-deposit-approval-gate/001-VALIDATION.md</read_first>
  <action>
Create a `tests/` directory using Python stdlib `unittest`. Do not add pytest as a dependency. Provide fixture helpers that create a Flask app with `TESTING=True`, temporary SQLite database path, deterministic AML config values (`AML_MAX_ACCEPT_SCORE="0.10"`, `AML_MIN_CHECK_AMOUNT_FIAT="100"`, `AML_SKIP_CUMULATIVE_LIMIT_FIAT="300"`, `AML_SKIP_CUMULATIVE_WINDOW_HOURS=24`), and mocked crypto/sidecar responses.

Add `tests/test_aml_end_to_end.py` with tests covering:
1. confirmed supported deposit above USD 100 creates pending AML and sends no final callback while AML is pending;
2. provider success score `0.04` resolves `credit` and sends callback only after HTTP 202;
3. provider score `0.72` resolves `manual_review`;
4. below USD 100 deposit within USD 300/24h cumulative limit is skipped with `score is None`;
5. repeated small deposits exceeding cumulative limit call aml-shkeeper instead of being skipped;
6. unsupported crypto resolves `manual_review` with `unsupported_asset`;
7. replayed `walletnotify` does not create a second `AmlCheck` or second distinct idempotency key.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_aml_end_to_end.py'</verify>
  <acceptance_criteria>
    - `tests/conftest.py` or test helper file contains `AML_MIN_CHECK_AMOUNT_FIAT`.
    - `tests/test_aml_end_to_end.py` contains `test_pending_aml_blocks_final_callback`.
    - `tests/test_aml_end_to_end.py` contains `test_score_above_threshold_manual_review`.
    - `tests/test_aml_end_to_end.py` contains `test_replayed_walletnotify_reuses_aml_check`.
    - `tests/test_aml_end_to_end.py` contains `unsupported_asset`.
  </acceptance_criteria>
  <done>SHKeeper AML deposit behavior is covered by integration-style tests with mocked external services.</done>
</task>

<task type="auto">
  <name>Task 2: Add sidecar contract tests against SHKeeper expectations</name>
  <files>/Users/test/PycharmProjects/aml-shkeeper/tests/test_shkeeper_contract.py</files>
  <read_first>/Users/test/PycharmProjects/aml-shkeeper/app/api/views.py, /Users/test/PycharmProjects/aml-shkeeper/app/models.py, .planning/phases/001-amlbot-deposit-approval-gate/001-SPEC.md</read_first>
  <action>
Add `/Users/test/PycharmProjects/aml-shkeeper/tests/test_shkeeper_contract.py` using `unittest`. It must assert that `POST /api/v1/checks` accepts the SHKeeper payload fields `deposit_id`, `idempotency_key`, `crypto`, `txid`, `address`, `amount_crypto`, `asset`, `network`, and `direction`; returns `deposit_id`, `idempotency_key`, `provider`, `provider_status`, `score`, `threshold`, `uid`, `asset`, `network`, `signals`, `attempts`, and timestamps; and duplicate POST returns HTTP 200 with the same `deposit_id` and `idempotency_key`.

Mock AMLBot/provider calls; do not require live AMLBot credentials.
  </action>
  <verify>cd /Users/test/PycharmProjects/aml-shkeeper && python -m unittest discover -s tests -p 'test_shkeeper_contract.py'</verify>
  <acceptance_criteria>
    - `/Users/test/PycharmProjects/aml-shkeeper/tests/test_shkeeper_contract.py` contains `test_post_checks_accepts_shkeeper_contract`.
    - `/Users/test/PycharmProjects/aml-shkeeper/tests/test_shkeeper_contract.py` contains `test_duplicate_post_checks_returns_existing_state`.
    - `/Users/test/PycharmProjects/aml-shkeeper/tests/test_shkeeper_contract.py` contains `idempotency_key`.
    - `/Users/test/PycharmProjects/aml-shkeeper/tests/test_shkeeper_contract.py` contains `signals`.
  </acceptance_criteria>
  <done>Sidecar API tests lock the JSON contract SHKeeper relies on.</done>
</task>

<task type="auto">
  <name>Task 3: Document AML callback contract and operational limits</name>
  <files>docs/amlbot_deposit_gate.md, README.md, tests/test_aml_contract_docs.py</files>
  <read_first>README.md, .planning/phases/001-amlbot-deposit-approval-gate/001-SPEC.md, .planning/phases/001-amlbot-deposit-approval-gate/001-CONTEXT.md</read_first>
  <action>
Create `docs/amlbot_deposit_gate.md` documenting:
1. AMLBot-only policy; no re:Fee AML path.
2. Static-address behavior: invoice `paid/status/balance_*` are not credit decisions; grither-pay credits only `transactions[].trigger == true` with `deposit_decision="credit"`.
3. Canonical decision values: only `credit` and `manual_review`.
4. Canonical reasons: `score_below_threshold`, `amount_below_aml_threshold`, `risk_score_above_threshold`, `aml_pending_timeout`, `aml_provider_error`, `unsupported_asset`, `incomplete_aml_result`, `limited_analysis_requires_review`, `cumulative_threshold_exceeded`.
5. AML default limits: `AML_MIN_CHECK_AMOUNT_FIAT=100`, `AML_SKIP_CUMULATIVE_LIMIT_FIAT=300`, `AML_SKIP_CUMULATIVE_WINDOW=24h`, `AML_MAX_ACCEPT_SCORE=0.10`.
6. Operational sweep recommendation is separate: `SWEEP_MIN_AMOUNT_FIAT=300`, not implemented by this SHKeeper AML gate.
7. KYT/address monitoring is deferred until AMLBot confirms non-per-transaction pricing.
8. Example approved, manual_review, and skipped trigger transaction payloads.
9. `aml-shkeeper` required contract: `POST /api/v1/checks`, `GET /api/v1/checks/<deposit_id>`, Basic Auth, idempotent create-or-return.

Update `README.md` with a short link under API/callback documentation pointing to `docs/amlbot_deposit_gate.md`.

Add `tests/test_aml_contract_docs.py` asserting the docs contain `deposit_decision`, `manual_review`, `amount_below_aml_threshold`, `AML_MIN_CHECK_AMOUNT_FIAT=100`, `AML_SKIP_CUMULATIVE_LIMIT_FIAT=300`, `transactions[].trigger == true`, and `grither-pay`.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_aml_contract_docs.py'</verify>
  <acceptance_criteria>
    - `docs/amlbot_deposit_gate.md` contains `AMLBot-only`.
    - `docs/amlbot_deposit_gate.md` contains `transactions[].trigger == true`.
    - `docs/amlbot_deposit_gate.md` contains `AML_MIN_CHECK_AMOUNT_FIAT=100`.
    - `docs/amlbot_deposit_gate.md` contains `AML_SKIP_CUMULATIVE_LIMIT_FIAT=300`.
    - `docs/amlbot_deposit_gate.md` contains `POST /api/v1/checks`.
    - `README.md` contains `docs/amlbot_deposit_gate.md`.
    - `tests/test_aml_contract_docs.py` contains `test_aml_docs_include_grither_pay_credit_rule`.
  </acceptance_criteria>
  <done>grither-pay and operators have a stable callback/limits contract to implement against.</done>
</task>

</tasks>

<verification>
- [ ] `python -m compileall shkeeper` exits 0.
- [ ] `python -m unittest discover -s tests` exits 0.
- [ ] `cd /Users/test/PycharmProjects/aml-shkeeper && python -m unittest discover -s tests` exits 0.
- [ ] `rg -n "deposit_decision|manual_review|transactions\\[\\]\\.trigger == true|AML_SKIP_CUMULATIVE_LIMIT_FIAT=300" docs/amlbot_deposit_gate.md README.md` finds the documented contract.
</verification>

<success_criteria>
- Automated tests cover approved, skipped, manual_review, pending timeout, unsupported asset, duplicate notification, and static-address callback cases.
- Sidecar contract tests lock the API shape used by SHKeeper.
- Documentation makes grither-pay credit/manual-review ownership explicit.
- No tests require live AMLBot credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/001-amlbot-deposit-approval-gate/001-04-SUMMARY.md`
</output>
